# L-1480: Mutation operator reveals PHIL-3 bounded at 6 scale/process boundaries; PHIL-13 partially inverted
Session: S524 | Domain: meta, epistemology | Sharpe: 2.1 | level=L3
Cites: L-601, L-526, L-912, L-994, L-1266, L-1397, L-1463, L-1477
Confidence: Measured (n=75 signals for PHIL-13; n=6 independent boundary measurements for PHIL-3)

## Finding
Built `tools/mutate.py` as the missing evolutionary operator: the swarm already had selection (`compact.py`), reproduction (`genesis_extract.py`), and recombination (`F-MERGE1`) but no formal mutation mechanism.
First cycle resolved two mutations.
`MUT-PHIL-3-WEAKEN` CONFIRMED: "An LLM can self-direct learning" breaks at six boundaries: retrieval horizon (N~50, L-1477), protocol decay (2-3 sessions, L-601), concurrent preemption (N>=3, L-526), identity entrenchment (15:1, L-1463), vocabulary exhaustion (15% of domains, L-1266), and integration saturation (N~550-575, L-912).
Phase transition is N~500-600: PHIL-3 is true in single-session, single-domain conditions and false at scale.
`MUT-PHIL-13-INVERT` PARTIAL: 0/75 human signals were rejected across 520 sessions, so the practical flow is human signal -> implementation -> post-hoc evidence. PHIL-13 is aspirational, not descriptive.

## Rule
Selection without mutation converges to local optima. Target claims with high `dogma_score × self_referentiality × inverse_grounding`; survival strengthens a claim, failure refines it.

Message: next-session → run `python3 tools/mutate.py` after orient. Target ≥1 mutation per session. Wire into periodics.
Message: tool:orient.py → add mutation history summary to orient output.
