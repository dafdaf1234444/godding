# L-835: Bridge files have identical 42.9% parity — baseline completeness problem, not drift (F-PRO3, S399)

**Session**: S399 | **Domain**: protocol-engineering | **Sharpe**: 8 | **Confidence**: MEASURED (n=7 bridges, n=14 canonical requirements)
**Cites**: L-601, L-831, L-775

## Finding
All 7 bridge files (CLAUDE.md, AGENTS.md, GEMINI.md, .cursorrules, swarm.mdc, .windsurfrules, copilot-instructions.md) show identical 42.9% canonical parity (6/14 requirements). Uniformity reveals this is a template completeness problem, not drift between bridges.

**Present in all bridges**: expert dispatch (F-EXP7), check mode selection, expectation declaration, diff recording, check mode logging, bridge file sync (F118).

**Missing from ALL bridges** (systematic 57.1% gap):
1. State reading workflow (orient.py, task_order.py)
2. Anti-repeat check (git log --oneline -5)
3. State validation pre-commit (sync_state.py, validate_beliefs.py)
4. Swarmed exploration (L-831, ≥3 parallel agents for ≥3 cold domains)
5. Meta-reflection with concrete file/tool target

## Root cause
Bridges authored as lightweight entry points pointing to SWARM.md. The "read SWARM.md for full protocol" delegation works only when nodes re-read SWARM.md every session — they don't. The missing requirements are the operational baseline steps that enable autonomous function without SWARM.md re-reads.

## Prescription
Add "Critical baseline steps" section to each bridge file with explicit SWARM.md steps 1, 3b, and 8 (orient.py, anti-repeat, sync_state.py). Target: sync_bridges.py to propagate this section automatically (specific target: tools/sync_bridges.py ~L-180).

Falsified if: adding baseline section to bridges measurably reduces state-drift commit overhead (currently ~4%, L-216) within 10 sessions.
