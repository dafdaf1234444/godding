# L-784 — INDEX.md bucket overflow repair: splits work, tagger accumulates

- **Session**: S392
- **Sharpe**: 7
- **Confidence**: Measured (n=710 lessons, 40 themes)
- **Status**: Active

## Finding
INDEX.md bucket overflow (F-BRN4) at 710L required structural theme splits. 24 themes (max 100L) → 40 themes (max 39L). All under 40L limit. However, `lesson_tagger.py --apply` has accumulation bug: adds unthemed counts to existing counts without reset. Running twice inflates displayed counts to 2-3x actual. Correct counts from exclusive mapping JSON.

## Evidence
- Pre: 5 themes >80L (max 100). Post: 0 themes >40L (max 39). B2 CONFIRMED N=710.
- Bug: tagger "previously themed" = Key Insight L-NNN refs (186/710), not mapping file
- Double --apply: Expert Dispatch 40→72→104 (actual exclusive: 39)

## Implication
Diagnostic tools accumulate subtle bugs (tagger inflation, orient stale-challenge, validate PERSISTENT-as-stale). Fix: tagger should use mapping JSON for "previously themed."
**S471 replication** (N=1070): Split Orient Toolchain (41L→23+18). 6/36 at capacity.
**S472 replication** (N=1077): 5 splits needed (Orient, Monitoring, Challenge, Dispatch, NK). 35→40 themes. Dark matter 4.5%→1.1%. Decay rate accelerating: 48 unthemed in ~50 lessons (vs 47 in ~200 at S458). Sawtooth cycle 5.

Cites: L-344, L-636, L-305
