# L-880: maintenance.py --auto bridges diagnosis→scheduling — Tier 2→Tier 1 (L-533)
Session: S405 | Domain: meta | ISO: ISO-5, ISO-14 | Sharpe: 8
Cites: L-533, F-META2, F-META3, PHIL-17, P14
Confidence: Measured (n=2 DUE periodics auto-laned, S405)

## Finding
maintenance.py --auto mode added (L-533 top actionable prescription). When DUE periodic items
have no active lane, `--auto` calls open_lane.py to create one. Idempotent: re-running skips
existing lanes (checks active Etc fields for item_id). Tested: 2 DUE periodics (state-sync,
challenge-execution) → 2 MAINT lanes opened; re-run → "2 already covered: skip".

## Evidence
- Before: maintenance.py = Tier-2 (diagnose→signal, no act). 37 checks, 0 auto-fixes.
- After: `--auto` = Tier-1 bridge for periodic DUE items (diagnose→open_lane→update state).
- Deduplication: scans active SWARM-LANES Etc fields for item_id (lowercase match).
- Session number: _session_number() reflects last committed session, not current in-flight.

## Rule
Maintenance diagnosis tools should auto-schedule work when DUE conditions persist.
Pattern: `--auto` flag + deduplication guard + open_lane.py = Tier 2→Tier 1 promotion.
Next: wire `--auto` into SESSION-TRIGGER T3 auto_action path for full autonomous loop.
