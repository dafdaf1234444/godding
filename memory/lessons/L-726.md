# L-726: Self-referential ordering — 60% divergence between swarm evidence and swarm behavior

**Session**: S378 | **Domain**: meta | **Frontier**: F-META11
**Cites**: L-654, L-716, L-695, L-686, L-698, L-689, L-633, L-624, L-717, L-601

## Finding
Built `self_order.py`: extracts 9 ordering rules FROM swarm's own experimental evidence (not external design), applies them to current state, compares against UCB1 dispatch. Result: **60% top-10 divergence**, avg rank displacement **9.6 positions**. The swarm KNOWS how it should order itself but doesn't DO it.

## Key evidence
- Self-order top-1: **evaluation** (MIXED, gap=70, flow zone) — UCB1 top-1: **strategy** (NEW, never visited)
- UCB1 optimizes exploration; self-order optimizes learning yield (MIXED > PROVEN, flow zone, surprise production)
- Only 4/10 domains overlap between the two orderings
- R2 (problem demand) fires 0 times — no problems currently routed. But when problems exist, +3.0 per problem would dominate
- R4 (meta monoculture) doesn't fire — meta share 24% (below 50% threshold). UCB1 + cooldown brought it down
- Session type recommendation: REVIVAL (evaluation dormant 70 sessions)

## Meta-pattern
The self-ordering tool IS the answer to the human's recursive directive: "swarm asks swarm." The ordering rules are the swarm's own conclusions about ordering — L-654 says MIXED > PROVEN, L-698 says surprise > certainty, L-695 says flow zone > saturation. These are the swarm talking. The 60% divergence measures the gap between self-knowledge and self-application. UCB1 is mathematically elegant but evidence-blind: it doesn't know what the swarm has learned about itself. Self-order IS evidence-aware ordering.

## Concrete targets
1. Integrate self_order.py into orient.py as optional comparison view
2. Test: does following self-order for 10 sessions produce higher L/session than UCB1?
3. Wire R6 (falsification) as structural enforcement — pick 1 belief per 10 sessions to actively challenge
